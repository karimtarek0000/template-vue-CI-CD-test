name: 02-new-work

run-name: Test MATRIX

on: workflow_dispatch

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        node_version: [16, 18, 20]
        exclude:
           - os: ubuntu-latest
             node_version: 20
        include:
            - os: macos-latest
              node_version: 21
    steps:
      - name: print
        run: echo This runs on ${{ matrix.os }} and run on node version ${{ matrix.node_version }}

