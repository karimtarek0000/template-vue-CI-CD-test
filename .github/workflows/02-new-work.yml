name: 02-new-work

run-name: Test MATRIX

on: workflow_dispatch

jobs:
  build:
    runs-on: matrix.os
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    steps:
      - name: print
        run: echo This runs on ${{ matrix.os }}

