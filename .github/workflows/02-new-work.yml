name: 02-new-work

run-name: Test marketplace

on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4.2.2

      - name: Print text
        uses: ./.github/actions/print-some-text
